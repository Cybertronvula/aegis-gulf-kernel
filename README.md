The current public repository is a Python prototype demonstrating the cryptographic architecture and compliance logic. The production C++20 kernel is in active development. The Python implementation produces the same cryptographic guarantees — AES-256-GCM authenticated encryption and SHA-256 HMAC audit chains and serves as a verifiable reference implementation for technical due diligence
